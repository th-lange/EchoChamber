spring:
  data:
    rest:
      base-path: /api
  datasource:
    url: jdbc:tc:postgresql:15:///echochamber
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
